const PROD = 'https://apiengine.mrreset.com';
const QA = 'https://dev-ngn.mrreset.com';
/** This is done temporarily while we get staging back up TODO Restore this to use vars and work properly with the different environments */
//export const flow_id = process.env.REACT_APP_ENVIRONMENT === 'PROD' ? 64 : 64;
//export const baseUrl = process.env.REACT_APP_ENVIRONMENT === 'PROD' ? PROD : QA;
export const flow_id = 64;
export const baseUrl = 'https://apiengine.mrreset.com';
export const MRRESET_ENGINE = 'http://engine.mrreset.com';
